import React from 'react';
import Feedback from '../applications/feedback/feedbackContainer';

export default () => {
  return (
    <div>
    <Feedback
      applicationId={1}
      cfeiId={1}
    />
  </div>
  )
}
