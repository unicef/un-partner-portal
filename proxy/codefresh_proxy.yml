version: '1.0'

steps:

    build_proxy:
        type: build
        image_name: unicef/unpp-proxy
        dockerfile: Dockerfile
        working_directory: ${{main_clone}}/proxy
        tag: ${{CF_BRANCH}}
